import profile from './profile';
import myschedule from './myschedule';
import manager from './manager';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [profile, myschedule, manager]
};

export default menuItems;
