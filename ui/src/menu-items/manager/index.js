import profile from '../admin/profile';
import myschedule from '../admin/myschedule';
import manager from 'menu-items/manager/manager';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [profile, myschedule, manager]
};

export default menuItems;
